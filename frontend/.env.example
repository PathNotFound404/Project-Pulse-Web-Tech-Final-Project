# Copy this file to .env.local and fill in the values for local development.
# In production, VITE_API_BASE_URL is injected at Docker build time via --build-arg.

VITE_API_BASE_URL=http://localhost:8080
